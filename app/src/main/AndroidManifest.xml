<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatMemeStickers">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            WhatsApp Sticker Content Provider.
            The authority MUST match: your applicationId + ".StickerContentProvider"
        -->
        <provider
            android:name=".StickerContentProvider"
            android:authorities="com.catmemestickers.StickerContentProvider"
            android:exported="true"
            android:readPermission="com.whatsapp.permission.STICKER_IMAGE_PROVIDER" />

    </application>

</manifest>
